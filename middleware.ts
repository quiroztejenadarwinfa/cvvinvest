// Middleware eliminado - usando localStorage para el idioma
// This file must exist but can be empty since we're using client-side language switching

export function middleware() {
  // Middleware disabled - using localStorage approach
}

